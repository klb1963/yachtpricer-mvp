Ниже — короткий, точный и актуальный STATUS SNAPSHOT, который идеально подходит как “начальная точка” для следующего спринта/чата.

⸻

✅ YachtPricer — STATUS SNAPSHOT (на момент закрытия этого чата)

1. Функционал

Все ключевые фичи MVP готовы и работают:

### Базовая функциональность:
	- Ввод и хранение данных по находящимся в управлении яхтам (название, характеристики, базовая цена, владелец, компания, локация).
	- Управление пользователями и ролями:
		- **ADMIN** — глобальные права.
		- **FLEET_MANAGER** — работает в рамках своей компании.
		- **MANAGER** — черновики и отправка на согласование.
		- **OWNER** — доступ к своей лодке, может утверждать скидку.
	- Авторизация через Clerk, `whoami`, RBAC-политики на фронте и бэке.
	- RBAC + orgId filtering полностью закрывает доступ к чужим яхтам/пользователям вне своей организации.
	- Настройка и сохранение фильтров для отбора яхт-конкуренток.
	- Получение яхт-конкуренток из NauSYS.
	- Поддержка 3-х языков (RU, EN, HR).

### Workflow согласования базовой цены на неделю
	- Экран **Pricing** (таблица / карточки).
	- Локальное редактирование скидки или итоговой цены.
	- Проверка превышения лимита скидки (max discount %) с подсветкой поля.
	- Согласование через статусы:
	- `DRAFT` → `SUBMITTED` → `APPROVED` / `REJECTED`.
	- Сохранение Комментария при смене статуса.
	- Логирование действий (`lastComment`, `lastActionAt`).
	- Ограничение прав кнопок по ролям и статусам.
	- Поддержка «фактических» цен (Actuals) и Max discount %.

✓ Авторизация и скрытие кнопки Sign in на HomePage
	SignedIn показываеьтся если пользователь не залогинен, когда залогинен кнопка заменяется на Start working.

✓ Получение яхт-конкуренток из NauSYS:
	- улучшено отображение найденных яхт модель-год-локация-цена

✓ Сортировка таблицы Price History на странице YachtDetailsPage.tsx
	Сортировка по колонкам:
		•	Week start
		•	Price
		•	Discount
		•	Date
	Поддерживается переключение направления (asc/desc).

✔ Поле Starting base price — редактируем только при создании новой яхты
✔ Max discount % — редактируем только при создании новой яхты
	Добавлено поведение:
		•	При создании яхты — starting base price редактируемое поле
		•	При редактировании — read-only, отображается в виде заблокированного поля
		•	Добавлены подсказки, ограничения, i18n-ключи
		•	Поле ограничено по ширине (не тянется на всю ширину блока)
	Все поля, которые запоминаются при создании яхты (startingBasePrice и maxDiscountPct), теперь технически гарантированно immutable, и бэкенд не позволяет их изменить при редактировании данных яхты.

✓ Полный i18n для локализации на 3-х языках
	Все добавленные ключи есть в:
		•	en/home.json
		•	ru/home.json
		•	hr/home.json
		•	en/ru/hr/yacht.json
	Использование в компонентах корректно.

✓ FLEET_MANAGER теперь может назначать менеджера для яхты, ранее это мог делать только ADMIN	

⸻

2. Кодовая база

✔ Фронтенд и бэкенд собраны без ошибок
✔ Все изменения закоммичены и отправлены
✔ CI/CD Actions автоматически деплоит новый билд на VPS по адресу https://sandbox.leonidk.de 

⸻

3. Оставшиеся задачи (в ожидании)

Сейчас нет блокирующих задач — ждём обратной связи от Виктории:
	•	UX и тексты для Workflow pricing
	•	Дополнительные требования к PriceHistory
	•	Возможные правки по процессу Submit → Approve → Reject
	•	Дополнительные поля по яхтам (если будут запрошены)

Сценарий LastMinute (менее 21 дня вперед от текущей даты)
	- измененеие стартовой базовой цены
	- изменение максимальной скидки
TEMP: logic not implemented yet, only recorded as requirement

Аналитика:
  - Линейные графики цен по неделям в течение сезона.
  - Сравнение YoY (те же недели разных лет).
  - План/факт по загрузке и доходам от чартера

⸻

🎯 С чего начнём следующий чат

В новом чате начнём с этого статуса, и от него будем двигаться следующим шагом:

👉 Самый логичный next step:
Интерфейс выбора недели + корректировка логики “last-minute base price” по правилу “3 недели до даты”.

Но начнём с того, что ты напишешь:
“Боб, продолжаем — задача такая…”

⸻
