✅ YachtPricer — прогресс по MVP

1. Что уже сделано

UI/UX и фронтенд
	•	Публичная HomePage:
		•	Лендинг с описанием продукта и сценария работы.
		•	Локализация EN / RU / HR.
		•	Переключатель языка в Navbar.

	•	Базовый рабочий интерфейс приложения:
		•	Dashboard с таблицей и карточками яхт, которые находятся в управлении
		•	Кнопка Scan/Поиск - поиск яхт-конкуренток в NauSYS
		•	Окно Competitor filters - настройка фильтров по географии и характеристикам яхт (категория, производитель->модель, характеристики)
		•	Сохранение наборов пресетов фильтров

	•	Экран Pricing с таблицей и карточками:
		•	Переключение режимов отображения (Table ↔ Cards).
	•	Логика согласования базовой цены на неделю (week base price):
		•	Workflow: DRAFT → SUBMITTED → APPROVED / REJECTED.
		•	Кнопки Submit / Approve / Reject с модальным окном для комментария.
		•	Отображение истории решения: lastComment + lastActionAt.
	•	UX-детали:
		•	Подсветка ошибки при вводе скидки выше допустимого лимита (красная рамка + подсказка).
		•	Поля с единицами измерения внутри инпутов (%, €).
		•	Автосейв скидки и финальной цены по событию blur.

Backend / API / права
	•	DTO и контроллеры приведены к единому виду (в т. ч. maxDiscountPercent).
	•	Валидация скидки и финальной цены на сервере.
	•	RBAC (Role-Based Access Control - контроль доступа на основе ролей):
		•	проверка прав по ролям и организациям (MANAGER, FLEET_MANAGER, ADMIN, OWNER);
		•	workflow кнопок учитывает права пользователя и организацию.
		•	Журнал комментариев и статусов (audit log) для отслеживания решений.

DevOps / инфраструктура
	•	CI/CD на GitHub Actions → автодеплой на VPS.
	•	Dockerized Postgres + Prisma-миграции.
	•	Локальная разработка в Docker-окружении.
	•	Seed-данные для пользователей, яхт и недель.

⸻

⏭ Что осталось/можно делать дальше:

1. Роли и multi-tenancy (несколько компаний пользуются продуктом)
	•	Довести multi-tenancy до конца (цепочка Company → Fleet → Yacht).
	•	Для владельца лодки (OWNER) отладить режимы:
		•	Active — может видеть и согласовывать цены;
		•	ViewOnly — только просмотр без действий;
		•	Hidden — полное отсутствие доступа к объекту.

2. Статистика цен по годам (YoY)
	•	Хранение и агрегация недельных снимков price_snapshot.
	•	Базовая YoY-аналитика:
		•	графики и таблицы по сезону;
		•	сравнение сезонов по неделям и загрузке.
		•	Возможное использование материализованных представлений в БД для тяжёлых отчётов.

3. Рекомендации по скидке
	•	MVP-алгоритм рекомендаций:
		•	простые схемы TOP1 / TOP3 (ориентир на цену конкурента ± Δ) - уже реализовано на уровне сравнения с конкурентами в NauSYS — TOP1/TOP3.
	•	Настройки:
		•	вынести шаг Δ в конфиг/настройку (per-fleet и/или per-boat).
	•	Подготовка к будущему ML (machine learning):
		•	сбор фактов о бронях (когда и по какой цене реально продали слот).

4. Учёт Last Minute
	•	Добавить флаг is_last_minute в price_snapshot.
	•	Отдельные витрины для:
		•	обычных недель;
		•	Last Minute предложений.
	•	Базовая эвристика для яхт-конкуренток:
		•	check-in < 30 дней и скидка ≥ 10–15% → считаем предложением Last Minute.

5. Интеграции:
	•	NauSYS API — основной источник цен конкурентов - уже реализовано.
	•	Сайт Aquatoria Group - автоматическое обновление цен на сайте Aquatoria Group через API или выгрузку.
	•	BookingManager API — по возможности, как дополнительный источник.
	•	CSV/Excel-импорт/экспорт.

6. Аналитика и ML (долгосрочно)
	•	Python-модуль / сервис для расчёта:
		•	средних значений, медиан, перцентилей, трендов по сезонам.
		•	ML-рекомендации:
	•	первые эксперименты с моделями типа XGBoost / RandomForest.
	•	Прозрачность для пользователя:
		•	объяснение рекомендаций в UI (почему именно такая цена/скидка).

⸻

✅ Итого

У нас есть полный рабочий цикл согласования скидок:
UI + API + RBAC + аудит + автодеплой.

📌 Основные следующие шаги (Sprint Goals):
  • Поддержка Last Minute и витрины быстрых предложений.
  • Реализация базовой YoY-аналитики.
  • Подготовка данных для ML-рекомендаций.